mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.videoDetail(video.id)) 2
        video.videoBlock__thumbnail(src=video.videoFile, controls=true)
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views 